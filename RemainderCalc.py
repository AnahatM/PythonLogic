# CALCULATES DIVISION WITH REMAINDER

divisor = float(input("Enter the divisor: "))
dividend = float(input("Enter the dividend: "))

print(f"ANS: {dividend/divisor}")
print(f"REMAINDER: {dividend//divisor} - R: {dividend%divisor}")